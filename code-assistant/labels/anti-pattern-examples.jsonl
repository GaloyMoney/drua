{"primary_slug":"check-then-insert","additional_slugs":[],"code":"    #[instrument(name = \"workspace_secret.create\", skip_all)]\n    pub async fn create(\n        &self,\n        workspace_id: WorkspaceId,\n        name: &str,\n        secret_type: SecretType,\n        value: &str,\n    ) -> Result<WorkspaceSecret, WorkspaceSecretError> {\n        let encrypted_value = self.encryption_key.encrypt_string(value);\n\n        // @@ no need to for this check\n        // just create - and let the index error on creation\n        let existing = self.list_all_for_workspace(workspace_id).await?;\n        let existing = existing.into_iter().find(|s| s.name == name);\n\n        if let Some(mut secret) = existing {\n            secret.update_value(encrypted_value);\n            self.repo.update(&mut secret).await?;\n            return Ok(secret);\n        }","source":"inline-review","source_id":"a5280c05dd33e25b66584af29b64e986bec878f5de81036b00aaefe2d697872e","original_comment":"no need to for this check","file":"core/src/workspace_secret/mod.rs","repo":"Galoy-agents","triaged_at":"2026-05-29T19:59:53Z"}
