Spaces feature — end-to-end smoke test. Goal: prove the full create→commit→search loop works for a brand-new library space, with no manual workarounds. Constraints (do not deviate): the sandbox must use mode "library_space" — do not fall back to scratch/repo, and if library_space fails, stop and report the exact error rather than working around it; every verification step must show evidence (a tool result you can quote) — do not summarize "the file is now available" without a search hit or a directory listing in your context; use top-level tools directly, and if you find yourself running search_tools more than twice, stop and ask. Sequence: (1) call the spaces tool with command=create, slug=oncall-test, description="e2e smoke" — capture the returned space_id; (2) call sandbox command=create, mode=library_space, space_slug=oncall-test, name=oncall-test-sb — the call blocks until state=ready by default (6-minute budget); quote the final state from the response; (3) call sandbox command=inspect, sandbox_id=<id>, tool=ls, tool_args={"path":"/workspace/library/spaces/oncall-test"} — confirm the directory exists and contains .gitkeep, and quote the listing; (4) create a workflow with name=oncall-test-workflow, manual=true, sandboxes=[{kind:"preexisting", name:"oncall-test-sb"}], and one agent step {name:"run-smoke-test", skill:"oncall-test-skill", sandbox:"oncall-test-sb"} — the step MUST reference the preexisting sandbox by name, otherwise the spawned sub-agent will have no sandbox access; the skill body spells out three actions: (a) create runbooks/hello.md containing "# Hello from oncall-test\n\nThis is the e2e marker.\n" via the text_editor create command at path /workspace/library/spaces/oncall-test/runbooks/hello.md, (b) run bash with `cd /workspace/library && git add -A && git -c user.email=agent@drua -c user.name=agent commit -m "add hello.md" && git push origin HEAD`, (c) verify with bash `git log --oneline -1 -- spaces/oncall-test/runbooks/hello.md` and quote the output; (5) call workflow command=trigger with the definition_id — capture the run_id; then call workflow command=await_run run_id=<id> which blocks until the run reaches a terminal state (6-minute budget); quote the final run state and the per-step output from the await_run response, no separate poll loop is needed; (6) wait up to 30s for the file indexer, then call library command=search, query="hello oncall", types=["space_file"] — the hit must include space_slug=oncall-test, relative_path=runbooks/hello.md, and if it doesn't, do not proceed but report the empty result; (7) call library command=get_files with the hit's id and confirm the body contains "This is the e2e marker." — quote the body. Reporting: at the end produce a numbered list, one line per step, format `[ok|fail] step_N: <evidence>` — do not say "everything worked" without that table.
