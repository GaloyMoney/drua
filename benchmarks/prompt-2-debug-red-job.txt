You are an on-call SRE. The drua-bin pipeline has a problem.

Pipeline: drua-bin

Your job:
1. List all jobs in the pipeline
2. Identify the critical path to production deployment (the chain of jobs that must succeed for code to reach prod)
3. Find any job on that critical path that is failing or errored
4. If you find one, get the build logs and analyze the root cause
5. If the critical path is clean, say so and note any non-critical failures separately

Produce a diagnosis in exactly this format:

## Pipeline Health — drua-bin

### Critical Path
<list the jobs in order that form the path to prod>

### Critical Path Status
- ✅ CLEAR — all critical jobs passing
- ❌ BLOCKED at <job name> — critical job failing

If a critical job is failing:

## Incident Report — drua-bin/<job>
**Build**: #<number> (ID: <build_id>)
**Status**: <status>
**Duration**: <seconds>s
**Previous build**: #<number> — <status>

### Root Cause
<1-3 sentence explanation of what went wrong>

### Relevant Log Excerpt
<the key 10-30 lines from the logs showing the failure>

### Suggested Fix
<1-2 sentence suggestion based on the error>

### Non-Critical Issues
<list any other failing jobs that are not on the critical path>
